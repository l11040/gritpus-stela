# GitHub Secrets에 등록할 항목:
#   DEPLOY_HOST    = 서버 IP
#   DEPLOY_USER    = SSH 유저
#   DEPLOY_SSH_KEY = SSH 개인키
#   DEPLOY_PORT    = SSH 포트 (기본 22, 생략 가능)
#   ENV_FILE       = 아래 내용 전체를 복사해서 값으로 넣기

DOMAIN=gritpus.neordinary.xyz
DB_ROOT_PASSWORD=
DB_DATABASE=gritpus
DB_USERNAME=gritpus
DB_PASSWORD=
